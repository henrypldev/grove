{
	"name": "server",
	"version": "0.14.0",
	"scripts": {
		"start": "NODE_ENV=production bun run src/index.ts",
		"dev": "bun build:simserver && NODE_ENV=development bun run --watch src/index.ts",
		"test": "bun test src/",
		"build:simserver": "cd simulator-server && swift build -c release && cd .."
	},
	"dependencies": {
		"@usegrove/shared": "workspace:*",
		"@anthropic-ai/claude-agent-sdk": "^0.2.50",
		"drizzle-orm": "^0.45.1",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"drizzle-kit": "^0.31.9"
	}
}
